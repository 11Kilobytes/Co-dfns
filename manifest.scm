;; TODO: Package arrayfire, and add a channels file that pulls in nonguix so that you
;; can replace the strings here with actual packages.

(specifications->manifest
 (list "dyalog-apl" "gcc-toolchain" "mold" "bash" "coreutils" "binutils" "sed" "glibc" "texinfo" "man-db" "gdb"))
